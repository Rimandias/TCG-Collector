import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.tcgcolecionador.app',
  appName: 'TCG Colecionador',
  webDir: 'dist'
};

export default config;
