# Chave da Pokemon TCG API (https://pokemontcg.io) - opcional, aumenta o rate limit.
# NUNCA exponha essa chave no frontend. Ela só é usada aqui no backend.
POKEMONTCG_API_KEY=

# Segredo usado para assinar os tokens JWT de autenticação. Gere um valor aleatório forte.
JWT_SECRET=troque-este-valor-por-um-segredo-forte-e-aleatorio

# Porta em que o backend vai rodar
PORT=8787

# Origem do frontend permitida pelo CORS
CLIENT_ORIGIN=http://localhost:3000
